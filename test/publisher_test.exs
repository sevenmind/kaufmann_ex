defmodule KaufmannEx.PublisherTest do
  use ExUnit.Case

  describe "&produce/3" do
    test "uses default topic from message metadata"
  end

  describe "&event_metadata/2" do
    test "with default 7m metadata"
    test "with custom metadata"
  end
end
